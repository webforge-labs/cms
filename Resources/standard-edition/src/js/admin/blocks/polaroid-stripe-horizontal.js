define(['knockout'], function(ko) {

  var vm = function(params) {};

  var template = '<p><i>Horizontal im Text für Tablet und Smartphone</i></p>';

  return { viewModel: vm, template: template };
});